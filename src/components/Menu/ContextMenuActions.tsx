import { Editor } from "@tiptap/react"

/**
 * Developer notes: What if you are currently viewing the file and it gets deleted?
 *      
 */
export const deleteFileItem = async (filePath: string | undefined) => {
    if (!filePath) return
}
const renameFileItem = () => {}
const createNewNote = () => {}
const createNewDirectory = () => {}
const createFlashcardSet = () => {}
const addFileToContext = () => {}
const deleteChat = () => {}
